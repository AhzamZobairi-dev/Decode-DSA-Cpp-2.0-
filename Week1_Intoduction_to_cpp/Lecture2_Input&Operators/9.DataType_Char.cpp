/*
Char data type stores a single character as text
using ' '

*/

#include <iostream>
using namespace std;

int main()
{
    char ch = '%';              // treats as a text not any operator
    char x = 'a';
    
    cout << ch << " " << x;     // if we are giving single space we 
                                //can use ' ' but for multiple gap 
                                //" " is required
    return 0;
}

